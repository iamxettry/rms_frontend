

export const metadata={
    title:"BubbleDubble | Login page",
    description:"Welcome to the BubbleDubble Login Page!"
}
const layout = ({children}) => {
  return (
    <>
        <main>{children}</main>
    </>
  )
}

export default layout